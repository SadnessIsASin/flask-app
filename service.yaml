apiVersion: v1
kind: Service
metadata:
  name: devops-exp-service
spec:
  type: NodePort
  selector:
    app: devops-exp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30007